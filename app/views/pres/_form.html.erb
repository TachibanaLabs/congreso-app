<%= form_with(model: pre, local: true) do |form| %>
  <% if pre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pre.errors.count, "error") %> prohibited this pre from being saved:</h2>

      <ul>
      <% pre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :correo %>
    <%= form.text_field :correo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
